<Window x:Class="TingenTransmorger.Database.MessageSummaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TingenTransmorger.Database"
        mc:Ignorable="d"
        Title="Message Summary" Height="600" Width="900"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!--  SMS Failures Section  -->
                <Border BorderBrush="Black" BorderThickness="1" CornerRadius="5" Margin="0,0,0,10" Padding="10">
                    <StackPanel>
                        <TextBlock FontSize="16" FontWeight="Bold" Text="SMS Failures" Margin="0,0,0,10"/>
                        <DataGrid
                            x:Name="dgSmsFailures"
                            Height="200"
                            MaxHeight="200"
                            AutoGenerateColumns="False"
                            CanUserAddRows="False"
                            CanUserDeleteRows="False"
                            CanUserReorderColumns="False"
                            CanUserResizeColumns="True"
                            CanUserResizeRows="False"
                            HeadersVisibility="Column"
                            IsReadOnly="True"
                            VerticalScrollBarVisibility="Auto">
                            <DataGrid.Columns>
                                <DataGridTextColumn
                                    Width="150"
                                    Binding="{Binding ScheduledStartTime}"
                                    Header="Scheduled Start Time" />
                                <DataGridTextColumn
                                    Width="*"
                                    Binding="{Binding ErrorMessage}"
                                    Header="Error Message" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <!--  Message Deliveries Section  -->
                <Border BorderBrush="Black" BorderThickness="1" CornerRadius="5" Padding="10">
                    <StackPanel>
                        <TextBlock FontSize="16" FontWeight="Bold" Text="Message Deliveries" Margin="0,0,0,10"/>
                        <DataGrid
                            x:Name="dgMessageDeliveries"
                            Height="250"
                            MaxHeight="250"
                            AutoGenerateColumns="False"
                            CanUserAddRows="False"
                            CanUserDeleteRows="False"
                            CanUserReorderColumns="False"
                            CanUserResizeColumns="True"
                            CanUserResizeRows="False"
                            HeadersVisibility="Column"
                            IsReadOnly="True"
                            VerticalScrollBarVisibility="Auto">
                            <DataGrid.Columns>
                                <DataGridTextColumn
                                    Width="100"
                                    Binding="{Binding DateSent}"
                                    Header="Date Sent" />
                                <DataGridTextColumn
                                    Width="80"
                                    Binding="{Binding TimeSent}"
                                    Header="Time Sent" />
                                <DataGridTextColumn
                                    Width="120"
                                    Binding="{Binding DeliveryStatus}"
                                    Header="Delivery Status" />
                                <DataGridTextColumn
                                    Width="100"
                                    Binding="{Binding MessageType}"
                                    Header="Message Type" />
                                <DataGridTextColumn
                                    Width="*"
                                    Binding="{Binding ErrorMessage}"
                                    Header="Error Message" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!--  Close Button  -->
        <Button
            x:Name="btnClose"
            Grid.Row="1"
            Width="100"
            Height="35"
            Margin="0,10,0,0"
            HorizontalAlignment="Center"
            Content="Close"
            FontSize="14"
            FontWeight="SemiBold"
            Click="btnClose_Click">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5" />
                </Style>
            </Button.Resources>
        </Button>
    </Grid>
</Window>
